apiVersion: v1
kind: Service
metadata:
  name: postgresbackup-svc
  labels:
    io.kompose.service: postgresbackup
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: health
      protocol: TCP
      name: health
  selector:
    io.kompose.service: postgresbackup
