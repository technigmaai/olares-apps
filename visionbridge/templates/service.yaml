apiVersion: v1
kind: Service
metadata:
  name: visionbridge
  namespace: '{{ .Release.Namespace }}'
spec:
  type: ClusterIP
  selector: { io.kompose.service: visionbridge }
  ports:
  - { name: http, port: 8080, targetPort: 8080 }