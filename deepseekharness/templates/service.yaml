apiVersion: v1
kind: Service
metadata:
  name: deepseekharness
  namespace: '{{ .Release.Namespace }}'
spec:
  selector:
    app: deepseekharness
  ports:
  - name: http
    port: 8080
    targetPort: 8080
    protocol: TCP
